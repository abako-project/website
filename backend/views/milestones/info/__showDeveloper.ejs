<div class="form-group">
    Developer:

    <% if (!milestone.developerId) { %>
        [Unassigned]
    <% } else { %>
        <img class="attachment"
             src="/developers/<%= milestone.developerId %>/attachment">

        <%= milestone.developer?.name ?? "Anonymous" %>

        <% if (milestone.state === "WaitingDeveloperAccept") { %>
            [Waiting For Developer Accept]
        <% } else if (milestone.state === "InProgress") { %>
            [In Progress]
        <% } else if (milestone.state === "ClientValidationNeeded") { %>
            [Client Validation Needed]
        <% } else if (milestone.state === "RejectedByClient") { %>
            [Rejected By Client]
        <% } else if (milestone.state === "Completed") { %>
            [Completed]
        <% } else { %>
            [Unknown state]
        <% } %>
    <% } %>

</div>
