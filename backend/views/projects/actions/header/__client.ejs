<%
const actions = [];

if (locals.loginUser.clientId == project.clientId) {

    if (["deployed"].includes(project.state)) {
        actions.push({
            title: "Review",
            classes: "",
            callback: "/projects/" + project.id + "/edit",
        });
    }

    if (["ProposalRejected"].includes(project.state)) {

        // Header actions
    }

    if (["EscrowFundingNeeded"].includes(project.state)) {
        actions.push({
            title: "Lock the Funds and Start the Project",
            classes: "btn_primary",
            callback: "/projects/" + project.id + "/start",
        });
    }
}
%>

<%- include("./__render", {actions}) %>
