<%
const actions = [];

if (locals.loginUser.isAdmin) {
    actions.push({
        title: "Delete",
        callback: "/projects/" + project.id + "?_method=DELETE",
    });


    if (project.state && ["ProposalPending"].includes(project.state)) {
        actions.push({
            title: "Assign Consultant",
            callback: "/projects/" + project.id + "/consultant/select",
        });
    }
}
%>

<%- include("./__render", {actions}) %>
